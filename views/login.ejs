<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        /* General styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #F8F8F9; /* Light White Lilac */
        }

        /* Container for centering the form */
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            box-sizing: border-box;
        }

        /* Form and illustration container */
        .form-container {
            display: flex;
            width: 100%;
            max-width: 1000px;
            background: rgba(255, 255, 255, 0.9); /* Semi-transparent white */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            overflow: hidden;
        }

        /* Form section */
        .form-section {
            flex: 1;
            padding: 20px;
        }

        /* Illustration section */
        .illustration-section {
            flex: 1;
            background: url('https://img.freepik.com/free-vector/tablet-login-concept-illustration_114360-7883.jpg') no-repeat center center;
            background-size: cover;
            min-height: 400px; /* Adjust based on your image size */
        }

        /* Heading styles */
        h1 {
            text-align: center;
            color: #002349; /* Dark Royal Blue */
            margin-bottom: 20px;
        }

        /* Form group styling */
        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #002349; /* Dark Royal Blue */
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .form-group input:focus {
            border-color: #957C3D; /* Gold */
            outline: none;
        }

        /* Button styling */
        button {
            width: 100%;
            padding: 10px;
            background-color: #957C3D; /* Gold */
            border: none;
            color: white;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            margin-bottom: 10px;
        }

        button:hover {
            background-color: #DAA520; /* Rich Gold */
        }

        button:active {
            background-color: #B8860B; /* Deep Gold */
        }

        /* Link styling for signup page */
        .signup-link {
            display: block;
            margin-top: 15px;
            text-align: center;
        }

        .signup-link a {
            color: #957C3D; /* Gold */
            text-decoration: none;
            font-weight: bold;
        }

        .signup-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-container">
            <div class="form-section">
                <h1>Login</h1>
                <form action="/login" method="POST">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" required>
                    </div>
                    <button type="submit">Login</button>
                    <button type="submit" name="admin" value="true">Login as Admin</button>
                    <div class="signup-link">
                        <a href="/signup">Back to Signup</a>
                    </div>
                </form>
            </div>
            <div class="illustration-section">
                <!-- Illustration is handled by CSS background property -->
            </div>
        </div>
    </div>
</body>
</html>
